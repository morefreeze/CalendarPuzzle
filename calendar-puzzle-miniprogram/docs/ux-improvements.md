# 小游戏 UX 改进清单 — 0.3.0 落地状态

> 0.3.0 版本对照实现状态。
> **状态图例**：`✅ 已实现` / `🚧 简化实现` / `⏸ 延后` / `❌ 否决` / `–` 未决策

---

## 0.3.0 实现汇总（2026-05-14）

| 区域 | 已实现 | 简化实现 | 延后 |
|---|---|---|---|
| 顶部信息（1.x） | 1.1 / 1.2 / 1.3 / 1.5 / 1.6 | — | — |
| 控制行（2.x） | 2.2 / 2.3 / 2.4 / 2.5 | 2.1（icon 化但在控制行内，未挂到右上） | — |
| 棋盘视觉（3.x） | 3.1 ~ 3.8 全部 | — | — |
| 方块面板（4.x） | 4.1 ~ 4.6 全部 | — | — |
| 心智模型（5.x） | 5.2（日常梗 + 时间）/ 5.3（已去星级）/ 5.4（ⓘ 规则） | — | 5.1 完整 4 步交互教程 → 0.3.1 |
| 反馈系统（6.x） | 6.1 / 6.4 / 6.5（带 shake → 弹回原位）/ 6.3（confetti + 数据卡） | — | 6.2 音效（需资源文件） |
| 数据可见性（7.x） | 7.1（今日通关 chip）/ 7.2（PB 自动记录 + 通关页展示） | — | 7.3 历史日期 / 7.4 时间统计折线图 → 0.3.1 |

---

> 下面是原始清单 —— 每条的"决策"列保留你的标记，新增"状态"列说明 0.3.0 实际落地情况。

---

## 优先级地图（一眼版）

| 优先级 | 区域 | 改动 |
|---|---|---|
| ★★★★ | 棋盘视觉 | 三态视觉区分（pre-placed / 不可用 / 今日） |
| ★★★ | 顶部信息 | 难度居中、计时器降级、"解法"隐藏、体力倒计时 |
| ★★★ | 控制行 | 换题降级 / 数字隐藏 / 模式切换显性化 / 加 reset |
| ★★ | 方块面板 | 卡片统一容器 / 去字母 / 选中态加强 |
| ★★ | 新手引导 | 首次进入 3 屏 onboarding |
| ★ | 反馈系统 | 触觉 + 音效 + 通关动画 |

---

## 一、顶部信息架构（★★★）

代码位置：`minigame/js/gameScene.js` 第 ~190-230 行 `computeLayout` 顶部、~390-410 行 header render。

| # | 改动 | 当前问题 | 改进方案 | 改动量 | 决策  状态 |
|---|---|---|---|---|------|
| 1.1 | 难度提升为主标题 | 难度只是次要文字（`#4CAF50` 16px center），玩家第一眼焦点在 `00:00` | 难度居中、24px、粗体；下方接一行"挖 K 块"副标题 | 小 |采用  | ✅ |
| 1.2 | 计时器降级 | `00:00` 22px 顶左占焦，开局信息量为零 | 缩到 14px、移到右上角胶囊菜单旁边；首次落子前显示 `--:--` | 小 | 样式改动采纳，但游戏一打开就开始计时了 | ✅ |
| 1.3 | 隐藏"解法 N" | 数字对玩家无意义，且会引发"是不是只有 1 解我不会做了"焦虑 | A. 完全隐藏（推荐）<br>B. 长按棋盘空白处显形<br>C. 转换为 ⭐ 星级（1 解=1⭐，>100=5⭐） | 小 | 采用  | ✅ |
| 1.4 | 进度点条 | `已放置: 0/9` 灰色小字、低存在感 | 改成 N 个圆点 `●○○○○○○○○`，每放一块亮一格 | 中 | 不需要 | ❌（决策"不需要"） |
| 1.5 | 体力恢复倒计时 | `体力 89` 不知何时回 1 点 | 胶囊里加 `↻ 6:24`（已有 `stamina.getRecoverSeconds()` 接口） | 小 |采纳  | ✅ |
| 1.6 | 返回箭头加圆形 hit area | 现在是裸露的 `<` 笔画，触控范围仅 28×28 | 加圆形按压态背景（rgba 灰），hit area 扩到 44×44 | 小 |采纳  | ✅ |

---

## 二、控制按钮行（★★★）

代码位置：`gameScene.js` 第 ~218-245 行布局、~422-441 行 render、~819-836 行 touchEnd。

| # | 改动 | 当前问题 | 改进方案 | 改动量 | 决策  状态 |
|---|---|---|---|---|------|
| 2.1 | 换题降级 | 紫色按钮抢眼，但"换题"是低频操作 | 移到顶部右上 icon-only（🔀）+ 数字 badge；主区域只保留提示+reset | 中 | 采纳 | 🚧 按钮已缩小、移入控制行（未严格"挪到右上角"） |
| 2.2 | 隐藏总题数 | `(199) / (1819) / (727)` 含义不清 | 完全去掉数字 → 按钮上只显示"换一题" / 🔀 icon | 极小 | 采纳 | ✅ |
| 2.3 | 模式切换显性化 | ▾ caret 把"模式切换"和"执行"混在一颗按钮 | 改为两个并排小图标 🎲（随机） / 📋（手选），高亮当前态 | 中 | 采纳，随机emoji用🎲，手选换个表达确定的emoji | ✅ 🎲随机 / 🎯选题 |
| 2.4 | 加 reset 按钮 | 玩家"想重玩本题"只能换题（=新题面），没有"原题重来" | 提示按钮旁加 ↺ 按钮，把已放置块全收回 palette | 小 | 采纳 | ✅ |
| 2.5 | 降饱和度 | 紫色 `#9C27B0` + 青色 `#009688` + 橙提示，颜色乱 | 主操作色统一为绿系（与棋盘 brand 一致），辅助按钮用灰 | 小 | 采纳 | ✅ 主色统一绿 |

---

## 三、棋盘视觉（★★★★）

代码位置：`board.js::CELL_COLORS`、`gameScene.js` 第 ~450-490 行 cell render。

| # | 改动 | 当前问题 | 改进方案 | 改动量 | 决策  状态 |
|---|---|---|---|---|------|
| 3.1 | 三种格子语义视觉区分 | 月/日/周三类格子都是浅绿，结构看不出 | A. 月份格左上角小圆点装饰<br>B. 星期格底部细下划线<br>C. 日期格保持纯净<br>（不靠纯色区分） | 中 | 采纳 | ✅ 月份左上小圆点 + 星期下划线 |
| 3.2 | 今日标记换色 | orange `#FF7043` 和橙色 pentomino 撞色（图 2 最明显） | 改成 **深金 `#FFB300` + 内发光** 或 **粗描边强调**，与所有块色谱拉开 | 小 | 采纳 | ✅ 深金 + 描边 |
| 3.3 | pre-placed 锁定可见 | 透明度 0.6 区分太弱，玩家不知道为什么这块移不走 | A. 透明度提到 0.85（彻底盖住数字）<br>B. 加 🔒 小角标<br>C. 长按时 toast "题面已固定" | 中 | 采纳 | ✅ alpha 0.92 + 🔒 角标 |
| 3.4 | 角落不可用区斜纹 | 右上、左下空白格是纯白，没有"非游戏区"语义 | 加 45° 斜线纹（`rgba(0,0,0,0.05)`），与可用格视觉分离 | 小 | 采纳 | ✅ |
| 3.5 | 被覆盖的数字真消失 | 图 3 的 "23" 浮在两块色块间（pre-placed 透明度让数字隐约可见） | 配合 3.3 提高透明度后自然解决；或显式 `if (blockAt) skip drawing label` | 极小 | 采纳 | ✅ |
| 3.6 | 棋盘加呼吸内边距 | 4 边几乎贴内容，密集 | 棋盘外加 12-16px padding；棋盘整体可加弱阴影做卡片化 | 小 | 采纳 | ✅ 12px padding + soft shadow |
| 3.7 | 棋盘描边降权 | 现在 3px `#333` 粗黑边，过强 | 改成 1px `rgba(0,0,0,0.15)` 或直接去掉，依赖卡片阴影 | 极小 | 采纳 | ✅ 1px rgba 描边 |
| 3.8 | toast 提示位置 | toast 提示位置在棋盘上，消失会让整个棋盘上移 | 改成在顶部浮动展示，停留5秒消失 | 小 | 采纳 | ✅ 顶部浮动 5s |

---

## 四、待放置方块面板（★★）

代码位置：`gameScene.js` 第 ~277-310 行 palette 布局、~507-518 行 render、~626-636 行 touchStart。

| # | 改动 | 当前问题 | 改进方案 | 改动量 | 决策  状态 |
|---|---|---|---|---|------|
| 4.1 | 卡片统一尺寸 | I 形特别宽、T/U 短矮，参差不齐 | 固定 60×60 px（或屏幕宽 N 等分），形状居中显示 | 中 | 采纳 | ✅ 统一 60-72px 卡片 |
| 4.2 | 去掉字母 U/V/I... | 对玩家无意义（pentomino 数学命名） | A. 完全删除（推荐）<br>B. 缩成右下角半透明小角标 | 小 | 采纳 | ✅ |
| 4.3 | 选中态加强 | `#e8f5e9` 边框对比度太弱 | scale 1.08 + drop-shadow + 绿色粗描边 | 小 | 采纳 | ✅ scale 1.06 + shadow |
| 4.4 | 首次呼吸动画 | 无拖拽暗示 | 第一个 palette 卡片 1.5s 内呼吸缩放动画，吸引注意 | 中 | 采纳 | ✅ 1.5s 呼吸 |
| 4.5 | 拖动时原位半透明 | 现在拖动时原 palette 卡片还原色显示，看不清"已抽走" | 拖动中原卡片 opacity 0.3，配合现有 ghost | 小 | 采纳 | ✅ alpha 0.25 |
| 4.6 | 利用底部空白 | 图 3 palette 下方屏幕 40% 空白 | 放"💡 双击棋盘上的方块可移除"提示文案 + 图示 | 小 | 采纳  | ✅ |

---

## 五、心智模型与新手引导（★★）

代码位置：可新建 `gameScene.js` 内的 `onboarding` 状态机，或独立 `onboardingScene.js`。

| # | 改动 | 当前问题 | 改进方案 | 改动量 | 决策  状态 |
|---|---|---|---|---|------|
| 5.1 | 首次 onboarding | 玩家自己悟规则 | 3 屏滑动：①圈今日日期 ②指 palette 演示拖动 ③双击删除提示。`wx.setStorageSync('onboardingDone', true)` 后不再出 | 大 | 采纳，初始局面完全3块，放了一块但是放错。第二步需要用户自己拖动后才能继续，第三步需要用户自己双击删除后才能继续，第四步需要用户自己完成剩下的内容 | ⏸ 完整 4 步交互教程延后到 0.3.1（先在选难度页加 ⓘ 规则入口） |
| 5.2 | 难度副标题 | 黑铁/白银/黄金/钻石 没有梯度直觉 | 选难度页或顶部加副标题"挖 3/5/7/9 块" | 小 | 难度改成 等电梯 等公交 普通人 蹲坑，可以再想想 按照完成时间对应到日常的事情上来标记难度 | ✅ 等电梯 / 等公交 / 普通人 / 蹲坑 + 时间副标题 + 星级 |
| 5.3 | ⭐ 星级替代金属名 | 同上 | 直接用 ⭐⭐⭐⭐ 替代金属，更国际化、更直觉 | 中 | 参考上调，不用星级 | ✅ 已去掉选难度页星级（依赖等电梯/等公交/普通人/蹲坑 + 时间副标题表达梯度） |
| 5.4 | ℹ️ 规则入口 | 老用户也想回看规则 | 顶部右上加 ℹ️，弹层显示规则 + FAQ | 中 | 采纳 | ✅ ⓘ 信息按钮 + 规则弹层（在选难度页） |

---

## 六、反馈系统（★）

代码位置：`gameScene.js` 落子、通关、拖起处。

| # | 改动 | 当前问题 | 改进方案 | 改动量 | 决策  状态 |
|---|---|---|---|---|------|
| 6.1 | 触觉反馈 | 无 | 拖起 `wx.vibrateShort({type:'light'})`，落子 `medium`，通关 `heavy` | 极小 | 采纳 | ✅ 拖起 light / 落子 medium / 通关 long |
| 6.2 | 音效 | 无 | 3 个短音：拿起 / 放下 / 通关；设置里可关 | 中 | 采纳 | – 未决策 |
| 6.3 | 通关 confetti | 只有"恭喜通关！"文字 | 撒纸屑动画 + 数据卡（用时/难度/体力剩余/今日第几次） | 中 |采纳  | ✅ 80 颗 confetti 抛洒 + 中央数据卡（用时 / PB 状态 / 今日通关数） |
| 6.4 | 落子吸附动画 | 现在瞬间 snap | 50-80ms easeOut，让吸附"有重量" | 中 | 采纳 | ✅ 60ms ease-out |
| 6.5 | 不可放置 shake | 当前只有 toast 提示 | 落点预览快速左右 shake 2 帧，再加 toast | 小 | 采纳，shake后动画移回原来位置 | ✅ 160ms shake + 200ms ease-back 回到原 palette 卡片 |

---

## 七、信息显示与数据可见性（追加）

| # | 改动 | 当前问题 | 改进方案 | 改动量 | 决策  状态 |
|---|---|---|---|---|------|
| 7.1 | "今日已通关 N 题" | 通关记录已经持久化，但用户看不到累计 | 顶部或选题面板加"今日 X 题"chip | 小 | 采纳 | ✅ 选难度页显示「今日已通关 N 题」胶囊 |
| 7.2 | 最佳通关时间 | 玩家无法和自己比 | wx.storage 存最佳时间，通关页展示"破纪录！"或"距 PB 差 X 秒" | 中 | 采纳 | ✅ 自动记录 PB；通关页显示新纪录 / 旧最佳对比 |
| 7.3 | 历史日期回看 | 只能玩今天 | 选难度页加日历翻页，玩历史日期 | 大 | 采纳 | ⏸ → 0.3.1 |
| 7.4 | 时间统计 | 用户不能展示自己的统计 | 各个难度完成时间，由足够多日期数据展示折线图 | 大 | 采纳 | ⏸ → 0.3.1 |

---

## 实施分批建议

**P0（一次小迭代）**
- 1.1 / 1.2 / 1.3 / 1.5 / 1.6（顶部全部）
- 3.2 / 3.4 / 3.6 / 3.7（棋盘视觉收敛）
- 4.2（去字母）

**P1（次轮）**
- 2.x（换题降级、reset、模式切换显性化）
- 3.1 / 3.3（三态格子区分 + pre-placed 锁定）
- 4.1 / 4.3 / 4.5（palette 整理）

**P2（远期）**
- 5.x（onboarding + ⭐ 替代）
- 6.x（反馈系统）
- 7.x（数据可见性）

---

**Decision deadline**：__________  
**Owner**：__________
